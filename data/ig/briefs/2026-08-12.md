# 📥 2026-08-12 아침 브리핑 — 창업·부업·수익화

> 수집 0건 → URL중복 제거 0건 → **이슈 0개** (니치 관련 0 / 무관 0)
> 중복 묶임 0개 · 과거 재등장 0개 · 분류 확인 필요 0개

## 🎯 오늘의 콘텐츠 후보 TOP 3

## 📂 카테고리별 신규 이슈

---

### 다음 단계
1. 위 후보 중 1건 선택 (또는 분류 확인 항목에서 승격)
2. 원문에서 수치·날짜·조건 확인 — 추측 금지
3. 릴스 슬라이드 카피 작성 — 7~9장 / 20~40초 / 후킹 → 맥락 → 본문 → 요약카드 → CTA
4. `node scripts/ig/render-reel.mjs --slug <슬러그>` → mp4 생성
5. `node scripts/ig/upload.mjs --slug <슬러그> --deploy` → 공개 URL
6. `node scripts/ig/publish.mjs --slug <슬러그>` → 확인 후 `--confirm` 으로 발행

---

### ⚠️ 수집 실패 — 네트워크 인프라 문제 (4일 연속, 최근 8일 중 6일)

15개 소스(`lib/feeds.json`) 전부 프록시 CONNECT 403. `NODE_USE_ENV_PROXY=1`로도 동일하게
`Proxy response (403) !== 200 when HTTP Tunneling` — 이 세션의 network egress 정책이
news.google.com·venturesquare.net·platum.kr·byline.network·outstanding.kr를 전부 막고 있음.

WebSearch(Anthropic 서버사이드 도구, 프록시 우회)는 정상 동작해 대체 조사를 시도함 —
"소상공인 경영안정바우처"(2026, 전국 대상, 25만원) 등 후보를 찾음. 그러나 원문 검증용
**WebFetch도 EGRESS_BLOCKED**(`www.bizinfo.go.kr`, `www.semas.or.kr` 모두 차단 확인) —
이 세션에서 외부 웹 원문을 하나도 열 수 없는 상태. 검색 스니펫만으로는 지원 대상 매출
기준·정확한 신청 마감일을 1차 출처로 확정할 수 없어("추측 금지" 원칙에 따라) 드래프트
작성을 보류함.

**08-09·10·11에도 동일 증상**이 커밋 로그에 기록되어 있음 — 인프라 조치가 아직 반영되지
않은 것으로 보임. 제안(우선순위 순):
1. 이 세션/환경의 network egress 허용 목록에 위 5개 RSS 호스트 + 원문 검증에 쓰는 정부·언론
   도메인(`bizinfo.go.kr`, `semas.or.kr`, 주요 언론사)을 추가
2. 또는 수집 단계(`collect.mjs`)를 egress 제한이 없는 별도 환경/스케줄러(GitHub Actions 등)로
   분리하고, 결과 파일만 이 세션이 읽어 판단하는 구조로 변경
3. 임시 완화책으로는 WebSearch만으로 카피를 작성하되 draft의 `note`에 "수치 미검증"을 명시하는
   방법이 있으나, 이 니치(지원금·마감일)는 오차가 곧 신뢰 손실로 이어져 권장하지 않음
